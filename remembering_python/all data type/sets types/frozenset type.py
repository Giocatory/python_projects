################ frozenset ################

# тоже часое что и set, но нельзя элементы: добавлять, удалять, изменять
# нельзя создавать пустой, какой смысл?

################ methods ################
# работают, как и с set

# len(fs)
# operation "in"
# fs.issubset(fs2)
# fs.issuperset(fs2)
# fs.union(fs2)
# fs.intersection(fs2)
# fs.difference(fs2)
# fs.copy(fs2)